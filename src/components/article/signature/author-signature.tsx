import { Fragment } from "react";

interface Props {
  publishDate: Date | null;
}

const AuthorSignature = ({ publishDate }: Props) => {
  return <Fragment></Fragment>;
};

export default AuthorSignature;
